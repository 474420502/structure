# orderedmap

```go
import "github.com/474420502/structure/map/orderedmap.go"
```

`map/orderedmap.go` is a placeholder directory for a future ordered map implementation.

## Current State

- The package declares an empty `OrderedMap` struct.
- There are no exported constructors.
- There are no public methods yet.

## Recommendation

Use [../linkedhashmap/doc.md](../linkedhashmap/doc.md) if you need stable insertion order today, or one of the tree-based structures under `tree/` if you need ordered-key traversal.